<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>PayFlow - Fast & Secure Payments</title>
  <style>
    /* Reset */
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }

    header {
      background: #0d47a1;
      color: #fff;
      padding: 1rem 2rem;
      text-align: center;
    }
    header h1 { font-size: 2rem; }
    header p { font-size: 1rem; margin-top: 0.5rem; }

    .hero {
      background: linear-gradient(to right, #1976d2, #42a5f5);
      color: #fff;
      padding: 4rem 2rem;
      text-align: center;
    }
    .hero h2 { font-size: 2.5rem; margin-bottom: 1rem; }
    .hero p { font-size: 1.2rem; margin-bottom: 2rem; }
    .hero button {
      background: #fff;
      color: #1976d2;
      border: none;
      padding: 0.8rem 2rem;
      font-size: 1rem;
      cursor: pointer;
      border-radius: 5px;
      transition: 0.3s;
    }
    .hero button:hover { background: #e3f2fd; }

    .features, .testimonials, .contact {
      padding: 3rem 2rem;
      text-align: center;
    }
    .features h3, .testimonials h3, .contact h3 {
      font-size: 2rem; margin-bottom: 1rem;
    }
    .feature-grid {
      display: flex;
      gap: 1.5rem;
      justify-content: center;
      flex-wrap: wrap;
      margin-top: 1rem;
    }
    .feature {
      flex: 1 1 250px;
      background: #f5f5f5;
      padding: 1.5rem;
      border-radius: 10px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }

    .testimonials .testimonial {
      max-width: 500px;
      margin: 1rem auto;
      background: #fafafa;
      padding: 1.5rem;
      border-left: 4px solid #1976d2;
      border-radius: 5px;
      font-style: italic;
    }
    .testimonial span {
      display: block;
      margin-top: 0.5rem;
      font-weight: bold;
      font-style: normal;
    }

    .contact form {
      max-width: 500px;
      margin: 0 auto;
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }
    .contact input, .contact textarea {
      padding: 0.8rem;
      border: 1px solid #ccc;
      border-radius: 5px;
    }
    .contact button {
      background: #1976d2;
      color: #fff;
      border: none;
      padding: 0.8rem;
      cursor: pointer;
      border-radius: 5px;
    }
    .contact button:hover { background: #0d47a1; }

    footer {
      background: #0d47a1;
      color: #fff;
      padding: 1rem;
      text-align: center;
    }
    footer a {
      color: #bbdefb;
      margin: 0 0.5rem;
      text-decoration: none;
    }

    /* Responsive */
    @media(max-width: 768px) {
      .hero h2 { font-size: 2rem; }
      .feature-grid { flex-direction: column; }
    }
  </style>
</head>
<body>

  <header>
    <h1>PayFlow</h1>
    <p>Fast. Secure. Seamless Money Transfers.</p>
  </header>

  <section class="hero">
    <h2>Send Money Anywhere, Anytime</h2>
    <p>Instant transfers with top-notch security, trusted by thousands worldwide.</p>
    <button>Get Started</button>
  </section>

  <section class="features">
    <h3>Why Choose PayFlow?</h3>
    <div class="feature-grid">
      <div class="feature">
        <h4>🔒 Secure</h4>
        <p>Advanced encryption keeps your transactions safe.</p>
      </div>
      <div class="feature">
        <h4>⚡ Fast</h4>
        <p>Send and receive money instantly with zero delays.</p>
      </div>
      <div class="feature">
        <h4>🌍 Global</h4>
        <p>Transfer money worldwide in just a few clicks.</p>
      </div>
    </div>
  </section>

  <section class="testimonials">
    <h3>What Our Users Say</h3>
    <div class="testimonial">
      “PayFlow made it so easy to send money to my family abroad!”
      <span>- Riya S.</span>
    </div>
    <div class="testimonial">
      “The fastest and most secure app I’ve ever used.”
      <span>- Arjun M.</span>
    </div>
  </section>

  <section class="contact">
    <h3>Contact Us</h3>
    <form id="contactForm">
      <input type="text" id="name" placeholder="Your Name" required />
      <input type="email" id="email" placeholder="Your Email" required />
      <textarea id="message" rows="5" placeholder="Your Message" required></textarea>
      <button type="submit">Send Message</button>
    </form>
  </section>

  <footer>
    <p>© 2025 PayFlow Inc. | Follow us:
      <a href="#">Facebook</a> | 
      <a href="#">Twitter</a> | 
      <a href="#">LinkedIn</a>
    </p>
  </footer>

  <script>
    // Simple form validation
    document.getElementById("contactForm").addEventListener("submit", function(e) {
      e.preventDefault();
      let name = document.getElementById("name").value.trim();
      let email = document.getElementById("email").value.trim();
      let message = document.getElementById("message").value.trim();
      
      if (!name || !email || !message) {
        alert("Please fill out all fields.");
        return;
      }
      if (!/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(email)) {
        alert("Please enter a valid email address.");
        return;
      }
      alert("Message sent successfully!");
      this.reset();
    });
  </script>

</body>
</html>
